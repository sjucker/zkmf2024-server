<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty name="smtpuser" source="logging.mail.smtp.user"/>
    <springProperty name="smtppassword" source="logging.mail.smtp.password"/>

    <appender name="emailAppender" class="ch.qos.logback.classic.net.SMTPAppender">
        <smtpHost>mail.netzone.ch</smtpHost>
        <smtpPort>587</smtpPort>
        <username>${smtpuser}</username>
        <password>${smtppassword}</password>
        <to>stefan.jucker@gmail.com</to>
        <subject>[ZKMF2024]: %logger{20} - %msg</subject>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg%n</pattern>
        </layout>
    </appender>

    <root level="ERROR">
        <appender-ref ref="emailAppender"/>
    </root>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
